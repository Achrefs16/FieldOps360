---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: minio-console
  namespace: fieldops-data
spec:
  entryPoints:
    - websecure
  tls: {}
  routes:
    - match: Host(`minio.fieldops.local`)
      kind: Rule
      services:
        - name: minio-console
          port: 9001
