# MinIO Console is exposed via NodePort (port 30901)
# Access: http://192.168.50.10:30901
# Path-based routing via Traefik does not work with MinIO's React SPA
# because it hardcodes asset paths at root level (/static/js/...)
