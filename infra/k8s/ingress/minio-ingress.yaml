---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: minio-console
  namespace: fieldops-dev
spec:
  entryPoints:
    - websecure
  tls: {}
  routes:
    - match: Host(`minio.fieldops.local`)
      kind: Rule
      services:
        - name: minio
          port: 9001
